name: Deploy Backend via aaPanel

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Send Deployment Trigger to aaPanel
        run: |
          curl -X GET "${{ secrets.AAPANEL_WEBHOOK_URL }}"